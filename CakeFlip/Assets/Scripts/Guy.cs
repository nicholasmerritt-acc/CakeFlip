using UnityEngine;

public class Guy : MonoBehaviour
{
    public Vector3 BoxColliderCenterValues = new Vector3(0f, 1.5f, 0f);
    public Vector3 BoxColliderSizeValues = new Vector3(1f, 3f, 1f);

    //public static event Action<Trick.TrickType> TrickCompleted; //dialogue completed or something? idk TODO some way to make the characters separate
}
